{
  "scripts": {
    "build:images": "node scripts/make-webp-variants.js",
    "check": "node scripts/check-balance.js && node scripts/check-inline-js.js && node scripts/make-security-headers.js --check",
    "test": "node --test tests/*.test.cjs tests/*.test.mjs",
    "check:reviews-live": "node scripts/check-review-feed.js",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "html-validate": "8.29.0",
    "playwright-core": "1.63.0",
    "sharp": "^0.35.4"
  },
  "dependencies": {
    "heic-convert": "^2.1.0"
  }
}
